#!/usr/bin/env bash

# Attempt connection via mosh
echo "Attempting connection via mosh"
mosh "$@"

# If mosh connection fails (returns non-zero value), attempt connection via SSH
if [[ $? -ne 0 ]]
then
	echo "Connection via mosh failed"
	echo "Attempting connection via ssh"
	ssh "$@"
fi
